# Database Configuration
DB_URL=jdbc:postgresql://localhost:5433/smart_diy_garage
DB_USERNAME=garage_user
DB_PASSWORD=garage_password

# Authentication (Used later in Phase 2)
JWT_SECRET=super_secret_jwt_key_that_should_be_at_least_256_bits_long
JWT_EXPIRY_MS=86400000
